import type { Assignment, Chore } from '../types'; import AssignmentCard from './AssignmentCard'; import EmptyState from './EmptyState';
export default function WeeklyBoard({assignments,chores,onGenerate,onToggle,onReroll,onChange}:{assignments:Assignment[];chores:Chore[];onGenerate:()=>void;onToggle:(a:Assignment)=>void;onReroll:(a:Assignment)=>void;onChange:(a:Assignment,choreId:string)=>void}){if(!assignments.length)return <EmptyState title="No chores assigned this week" text="Generate a fair chore schedule for the current week." action={onGenerate} label="Generate Chores"/>;return <div className="assignment-list">{assignments.map(a=><AssignmentCard key={a.id} a={a} chores={chores} onToggle={()=>onToggle(a)} onReroll={()=>onReroll(a)} onChange={id=>onChange(a,id)}/>)}</div>}
