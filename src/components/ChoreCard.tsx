import { Edit3, Trash2 } from 'lucide-react'; import type { Chore } from '../types'; const labels=['','Very Easy','Easy','Moderate','Hard','Heavy'];
export default function ChoreCard({chore,onEdit,onDelete}:{chore:Chore;onEdit:()=>void;onDelete:()=>void}){return <article className="card"><div className="card-head"><div><h3>{chore.name}</h3><span className={chore.active?'badge success':'badge'}>{chore.active?'Active':'Inactive'}</span></div><div className="icon-actions"><button onClick={onEdit}><Edit3 size={17}/></button><button onClick={onDelete}><Trash2 size={17}/></button></div></div><p>{chore.description||'No description'}</p><div className="difficulty"><span>Difficulty {chore.difficulty}</span><strong>{labels[chore.difficulty]}</strong></div></article>}
