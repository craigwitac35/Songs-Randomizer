import { Edit3, Trash2 } from 'lucide-react'; import type { Assignment, Resident } from '../types';
export default function ResidentCard({resident,assignment,onEdit,onDelete}:{resident:Resident;assignment?:Assignment;onEdit:()=>void;onDelete:()=>void}){return <article className="card resident-card"><div className="card-head"><div><h3>{resident.name}</h3><div className="badges"><span className={resident.active?'badge success':'badge'}>{resident.active?'Active':'Inactive'}</span><span className={resident.unavailable?'badge warning':'badge'}>{resident.unavailable?'Unavailable':'Available'}</span></div></div><div className="icon-actions"><button aria-label="Edit resident" onClick={onEdit}><Edit3 size={17}/></button><button aria-label="Delete resident" onClick={onDelete}><Trash2 size={17}/></button></div></div><div className="card-section"><span>Current Chore</span><strong>{assignment?.choreName||'Not assigned'}</strong></div><div className="muted">{resident.exemptChoreIds.length} chore exemption{resident.exemptChoreIds.length===1?'':'s'}</div></article>}
