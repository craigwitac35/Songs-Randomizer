import { Home, Users, ClipboardList, History, Settings } from 'lucide-react'; import type { View } from '../types';
export default function Sidebar({view,setView}:{view:View;setView:(v:View)=>void}){const items:[View,string,any][]=[['dashboard','Dashboard',Home],['residents','Residents',Users],['chores','Chores',ClipboardList],['history','History',History],['settings','Settings',Settings]];return <aside className="sidebar"><div className="brand"><div className="brand-mark">CF</div><div><strong>ChoreFlow</strong><small>Fair household chores</small></div></div><nav>{items.map(([v,label,Icon])=><button key={v} className={view===v?'active':''} onClick={()=>setView(v)}><Icon size={18}/>{label}</button>)}</nav><div className="sidebar-foot"><strong>ChoreFlow</strong><span>Fair household chore assignments</span></div></aside>}
