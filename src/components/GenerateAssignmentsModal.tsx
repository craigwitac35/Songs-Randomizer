export default function GenerateAssignmentsModal({open,week,residentCount,choreCount,existing,onClose,onGenerate}:{open:boolean;week:string;residentCount:number;choreCount:number;existing:boolean;onClose:()=>void;onGenerate:()=>void}){if(!open)return null;return <div className="modal-backdrop"><div className="modal"><h2>{existing?'Regenerate Weekly Assignments':'Generate Weekly Assignments'}</h2><p><strong>{week}</strong></p><div className="summary-box"><span>Available Residents <strong>{residentCount}</strong></span><span>Active Chores <strong>{choreCount}</strong></span></div>{existing&&<p className="warning-text">Assignments already exist for this week. Generating again will replace the current schedule.</p>}<ul className="rule-list"><li>Balance workload</li><li>Avoid recent repeats</li><li>Respect exemptions and availability</li></ul><div className="modal-actions"><button onClick={onClose}>Cancel</button><button className="primary" onClick={onGenerate}>{existing?'Regenerate':'Generate'}</button></div></div></div>}
