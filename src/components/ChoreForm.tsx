import { useEffect, useState } from 'react'; import type { Chore } from '../types';
export default function ChoreForm({open,chore,onSave,onClose}:{open:boolean;chore?:Chore;onSave:(c:Omit<Chore,'id'|'createdAt'|'frequency'>)=>void;onClose:()=>void}){const [name,setName]=useState('');const [description,setDescription]=useState('');const [difficulty,setDifficulty]=useState(3);const [active,setActive]=useState(true);useEffect(()=>{setName(chore?.name||'');setDescription(chore?.description||'');setDifficulty(chore?.difficulty||3);setActive(chore?.active??true)},[chore,open]);if(!open)return null;return <div className="modal-backdrop"><form className="modal form-modal" onSubmit={e=>{e.preventDefault();if(name.trim())onSave({name:name.trim(),description:description.trim(),difficulty,active})}}><h2>{chore?'Edit Chore':'Add Chore'}</h2><label>Chore Name<input value={name} onChange={e=>setName(e.target.value)} autoFocus/></label><label>Description<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={3}/></label><label>Difficulty<select value={difficulty} onChange={e=>setDifficulty(Number(e.target.value))}>{['Very Easy','Easy','Moderate','Hard','Heavy'].map((x,i)=><option key={x} value={i+1}>{i+1} — {x}</option>)}</select></label><label className="check"><input type="checkbox" checked={active} onChange={e=>setActive(e.target.checked)}/> Active</label><div className="modal-actions"><button type="button" onClick={onClose}>Cancel</button><button className="primary">Save Chore</button></div></form></div>}
